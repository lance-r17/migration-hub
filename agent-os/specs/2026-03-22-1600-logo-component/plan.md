# Plan: Logo Component + Sidebar Integration

## Tasks
1. Create `src/components/shared/Logo.tsx` — wraps `src/assets/vite.svg` with optional `className` prop
2. Update `AppSidebar.tsx` — replace `<LayersIcon>` with `<Logo className="size-5" />`
