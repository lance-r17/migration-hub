# Sign-Off Authorization — Plan

## Changes

1. `mock.ts` — Fix TL/BO approval userId placeholders to real user IDs; swap devPersonas BO to Karen Lee (u12)
2. `ProjectDetailsPage.tsx` — Replace `currentUserRole` derivation with project-assignment check
